{
  "mcpServers": {
    "neuralseek": {
      "type": "http",
      "url": "${NS_MCP_URL}",
      "headers": {
        "x-ns-instance": "${NS_INSTANCE}",
        "x-ns-apikey": "${NS_API_KEY}",
        "x-ns-url": "${NS_BASE_URL}"
      }
    }
  }
}
